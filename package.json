{
  "name": "railway-db-backup",
  "version": "1.0.0",
  "main": "backup.js",
  "scripts": {
    "backup": "NODE_ENV=production node /app/backup.js backup",
    "backup-data": "NODE_ENV=production node /app/backup.js backup-data",
    "restore": "NODE_ENV=production node /app/backup.js restore"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.901.0",
    "dotenv": "^17.2.3"
  }
}
